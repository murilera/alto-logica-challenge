export interface User {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
}

export interface UserGQLResponse {
  _id: string;
  firstName: string;
  lastName: string;
  email: string;
}
